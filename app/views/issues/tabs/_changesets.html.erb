<%= render :partial => 'commit_context/revisions', :locals => {
      :changesets => changesets,
      :project => project,
      :after_row => ->(changeset) { call_hook(:view_issues_history_changeset_bottom, { :changeset => changeset }) }
    } %>
